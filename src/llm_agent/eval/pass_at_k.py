import math

def estimate_pass_at_k(n: int, c: int, k: int) -> float:
    """Approximate pass@k from the HumanEval paper.

    Args:
        n: number of samples
        c: number of correct samples
        k: number of draws (usually 1)
    Returns:
        float: estimated pass@k
    """
    # TODO replace with the proper estimator.
    pass
